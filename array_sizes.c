const i64 S = 2;  //symbol types
const i64 N = 6;  // neighborhood positions
const i64 K = S*S*S*S*S*S; //key symbols
const i64 X = K; // row positions
const i64 T = 64; // text length
const i64 R = 10; // default encryption rounds
